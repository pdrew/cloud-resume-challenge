{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "cypress": "cypress open",
    "e2e": "start-server-and-test dev http://localhost:3000 \"cypress open --e2e --env RESUME_SITE_URL=http://localhost:3000\"",
    "e2e:headless": "start-server-and-test dev http://localhost:3000 \"cypress run --e2e --env RESUME_SITE_URL=http://localhost:3000\"",
    "smokeTest": "cypress run --e2e"
  },
  "dependencies": {
    "next": "latest",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "swr": "^2.1.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "cypress": "^12.9.0",
    "postcss": "^8.4.24",
    "start-server-and-test": "^2.0.0",
    "tailwindcss": "^3.3.2"
  }
}
